/** @format */

import { Request, Response } from "express";

const otpSentResetPasswordController = (req: Request, res: Response) => {
    res.json({ message: "otpSentRegistrationController controller" });
};

export default otpSentResetPasswordController;
